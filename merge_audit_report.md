# مذكرة دمج وتدقيق نظام نقاط البيع

**الأرشيف الأساس:** `pos_project-mobile-settlement-period-fix-final.zip`  
**مجلد العمل:** `/home/ubuntu/pos_project_mobile_accounting_audited`  
**تاريخ التحديث:** 26 أغسطس 2026  
**طبيعة العمل:** تدقيق وإصلاح محلي فقط؛ لم يتم نشر Worker ولم تُعدّل Turso الإنتاجية.

## النتيجة التنفيذية

أصبحت نسخة العمل تجمع إصلاحات التدقيق المحاسبي السابقة مع تطبيق الهاتف وتسوية المندوب واستلام الطلبات غير المستلمة، إضافة إلى ميزات دورة السندات والمرتجعات والمخزون المطلوبة في آخر طلب. تم الحفاظ على اتجاهات القيود المحاسبية، وتُحفظ السجلات الملغاة مع إنشاء قيد عكسي بدل حذف الأثر التاريخي.

تم تنفيذ العمل داخل الملفات المحلية فقط. لذلك لا تعني نتيجة الفحوص أن النسخة العامة أو قاعدة البيانات الإنتاجية قد تغيرت؛ يلزم نشر الواجهة وWorker معًا بعد مراجعة مستقلة، ثم إجراء مطابقة قراءة فقط للأرصدة الفعلية.

## الميزات الجديدة المنفذة

| المجال | التنفيذ |
|---|---|
| فواصل آلاف المبالغ | توسعة `number-utils.js` لتطبيع الأرقام العربية والفارسية وفواصل الآلاف، وإظهار الفواصل في الحقول الرقمية، مع إزالة الفواصل عند التركيز والإرسال حتى تصل القيم الرقمية الخام إلى JavaScript وAPI. أضيف المرجع الناقص إلى `inventory.html`، وتحققت جميع الصفحات الرقمية من تحميل المكتبة. |
| مرتجعات الموردين | إضافة `GET /suppliers/returns/:id` لقراءة رأس المرتجع وأسطر المنتجات مع اسم المنتج والكود/الباركود والكمية وسعر الوحدة والإجمالي ورقم فاتورة الشراء والعملة. أُبقي المسار محميًا بصلاحية `suppliers.view`. |
| طباعة المرتجع | تعديل `viewReturnDetail` في `suppliers.html` لطلب التفاصيل الفعلية وعرض جدول المنتجات داخل نافذة المرتجع، مع إبقاء زر الطباعة وربطه بـ`POSDocs.printReport`. |
| المخزون المتبقي للمورد | إضافة `remaining_value = quantity × last_purchase_price` لكل صف، وإضافة `summary.total_value` لمسار المورد المحدد، مع عرض الإجمالي في رأس التبويب وتذييل الجدول وزر طباعة التقرير. |
| سندات موحدة | تحويل `payments.html` إلى نموذج واحد يدعم السند العام، وسند العميل، وسند المورد، ونوعي القبض والصرف، مع اختيار نقد/محفظة للطرف، واختيار العملة والمبلغ والبيان. أضيف سجل موحد من `cash_vouchers` و`customer_payments` و`supplier_payments` عبر `GET /vouchers/all`. |
| طباعة السند | إضافة زر طباعة مباشر لكل صف في سجل السندات الموحد عبر `POSDocs.printVoucherData`، مع عرض اسم الطرف، النوع، العملة، طريقة الدفع، المحفظة، البيان، والقيمة الأساسية عند توفرها. |
| سندات العملاء | إضافة migration للحقل `customer_payments.type` بقيمة افتراضية `receipt`، ودعم `receipt` و`payment` في الإنشاء والإلغاء. سند قبض العميل: الصندوق/المحفظة مدين والذمم المدينة دائنة، ويخفض رصيد العميل. سند صرف العميل: الذمم المدينة مدينة والصندوق/المحفظة دائنة، ويزيد رصيد العميل. |
| كشف العميل | تحديث كشف العميل ليحافظ على توافق `customers.html` القديم؛ يظهر قبض العميل في الكشف بالنوع التاريخي `payment`، بينما يميز الصرف الجديد داخليًا كـ`customer_payment` في حسابات الرصيد والإجماليات. |
| سندات الموردين | الحفاظ على دعم `receipt` و`payment`، ورسوم التحويل التاريخية، وترحيل الرسوم إلى حساب **خسائر صرف العملات 6300**، مع عكسها عند الإلغاء. |

## الإصلاحات المحاسبية المحفوظة

| المجال | ما تم تدقيقه أو إصلاحه |
|---|---|
| القيود والعكس | تقوية `checkBalance`، رفض NaN وInfinity والسطور الصفرية والسطر المدين والدائن معًا، وإنشاء قيود عكسية موثقة عند الإلغاء. |
| الصندوق والمحافظ | عكس حركات الصندوق والمحفظة بدل الحذف، تهيئة أرصدة المحافظ قبل الزيادة، والالتزام بأنواع `wallet_transactions` المسموحة: `deposit` و`withdraw`. |
| العملات | حفظ أسعار الصرف التاريخية لمكونات الدفع، حساب القيم الأساسية دون ضرب مزدوج، ودعم مصارفة المحفظة والصندوق مع تحديد عملة المصدر والهدف ومبلغ المصدر. رسوم المصارفة مخفية وممنوعة، ورسوم التحويل العادية تذهب إلى 6300. |
| الإقفال وإعادة الفتح | تقييد العمليات بالفترة المغلقة، إنشاء قيد عكسي موثق عند إعادة الفتح، والحفاظ على حالة وسجل العملية. |
| التقارير | توحيد القيمة الأساسية، استبعاد الحركات الملغاة، وإبقاء تقارير العملاء والموردين والصندوق والمحافظ والمخزون متسقة مع حدود التاريخ المحلية لليمن. |
| الهاتف والمندوب | الحفاظ على تسوية المندوب بالفترة، مستحق COD، رسوم التوصيل عند الاستلام، واستلام الطلبات غير المستلمة دون تغيير الرصيد المالي بالخطأ. |

## التحقق المحلي

نجح `npm run check`. كما نجحت جميع اختبارات Python وJavaScript الموجودة في مجلد المشروع، بما فيها اختبارات القيود، العكس، العملات، رسوم التحويل، سندات الموردين، مصارفة الصندوق، المحافظ، الإقفال، التقارير، SQLite، الفلاتر، الهاتف، مجموعات المنتجات، والتكامل مع الموقع. أضيف اختبار `test_unified_features.py` لعقد الميزات الجديدة، واختبار `test_frontend_inline.mjs` الذي تحقق من تفسير **24 كتلة JavaScript مضمنة** واختبارات التطبيع والتنسيق الرقمي.

نجح أيضًا فحص صياغة Worker بوصفه ESM، ونجح `npx wrangler deploy --dry-run --outdir=/tmp/pos-wrangler-dry-run` دون نشر فعلي: **873.20 KiB** حجم الرفع و**147.82 KiB** بعد الضغط، مع `No bindings found` في dry-run.

تم تحديث harness اختبارات الفلاتر والتاريخ ليتبع عقد التطبيق الحالي: الإصدار الثالث لتخزين الفلاتر، تاريخ `Asia/Aden`، وحدود SQL المحلية نصف المفتوحة مع `datetime(..., '-03:00')`. هذا تحديث لاختبار قديم كان يتوقع عقدًا سابقًا، وليس تخفيفًا للتحقق.

## ملاحظات تشغيلية مهمة

تحويل `input[type=number]` إلى حقل نصي عند تفعيل العرض المجمّع ضروري لإظهار الفواصل، ولذلك لا يعتمد المتصفح وحده على `min` و`step` بعد التحويل. جرى الحفاظ على `inputmode` والتطبيع، وتبقى التحققات الرقمية النهائية في JavaScript وWorker؛ يجب اختبار سيناريوهات الكمية العشرية والحدود في بيئة القبول قبل الاعتماد.

صفحة السندات الموحدة لا تتجاوز صلاحيات الطرف: إنشاء سند عميل يمر بصلاحية `customers.payments`، وإنشاء سند مورد يمر بصلاحية `suppliers.finance.view`، والسجل الموحد يمر بصلاحية `vouchers.view`. أما صلاحية الإلغاء فتظل مستقلة ومحمية.

## القيود قبل الاستخدام الفعلي

هذه النسخة لم تُنشر تلقائيًا ولم تُنفذ أي عملية مالية على الإنتاج. قبل التشغيل الفعلي يجب نشر Worker والصفحات المتوافقة، تطبيق migration `customer_payments.type` على قاعدة اختبار ثم الإنتاج وفق إجراء تغيير معتمد، والتحقق من عدم وجود تعارض مع بنية قاعدة Turso الحالية.

يلزم كذلك إجراء **مصالحة قراءة فقط** بين دفتر الأستاذ، ميزان المراجعة، الصندوق، المحافظ، أرصدة العملاء والموردين، والمخزون. كما يجب مراجعة سياسة إعادة تقييم العملات وتقرير التقادم مع محاسب مؤهل؛ هذه المذكرة **تحليل تقني محاسبي وليست اعتمادًا من محاسب قانوني أو مدقق مستقل**.

## ملفات الاختبارات المضافة أو المحدثة

| الملف | الغرض |
|---|---|
| `test_unified_features.py` | تحقق ساكن من عقد السندات الموحدة، اتجاهات قيود العميل، تفاصيل المرتجع، قيمة المخزون، والطباعة والفواصل. |
| `test_frontend_inline.mjs` | تفسير كتل JavaScript المضمنة واختبار التطبيع والتنسيق الرقمي. |
| `test_filter_bridge.js` | تحديث مفتاح التخزين إلى v3 وتاريخ الاختبار إلى `Asia/Aden`. |
| `test_worker_date.js` | تحديث عقد حدود التاريخ ليستخدم الإزاحة `-03:00`. |
| `test_worker_query.js` | حقن `businessISODate` في harness الخاص بالـpaging. |

## الخلاصة

الحزمة المحلية اجتازت فحوص الصياغة والاختبارات المحاسبية والساكنة وdry-run، وتحتوي الميزات المطلوبة دون نشر أو تعديل إنتاجي. يبقى الاعتماد النهائي مشروطًا بنشر منضبط ومصالحة قراءة فقط للبيانات الفعلية ومراجعة محاسب مؤهل.


## تحديث سجل حركات المحافظ والصندوق — 26 أغسطس 2026

تمت إضافة سجل تفصيلي لحركات المحافظ في `cash-wallets.html` مع endpoint `GET /wallets/transactions`. يدعم السجل اختيار محفظة محددة أو عرض كل المحافظ، والتصفية حسب `deposit` أو `withdraw`، والتصفية حسب تفاصيل العملية مثل الإيداع من الصندوق، السحب إلى الصندوق، التحويل بين المحافظ، المصارفة، المبيعات، المشتريات، المصروفات، سندات العملاء والموردين، ورسوم أو خسائر الصرف. يعرض الجدول التاريخ، اسم المحفظة، النوع، التفاصيل العربية، المبلغ، العملة، البيان التفصيلي، ورقم المرجع.

كما تمت إضافة سجل تفصيلي لحركات الصندوق عبر `GET /cash/transactions`. يدعم التصفية حسب نوع الحركة، العملة، وتفاصيل العملية مثل الفتح والإغلاق والتحويل بين الصندوق والمحفظة والمصارفة والمبيعات والمشتريات والمصروفات وسندات العملاء والموردين والعمليات اليدوية. يعرض الجدول المبلغ والقيمة الأساسية وسعر الصرف والبيان، مع زر طباعة مستقل لكل سجل.

تعتمد الواجهتان على pagination في Worker، وتعيدان عدد النتائج والفلترة المستخدمة، وتستخدمان `POSDocs.printReport` للطباعة. بقيت مسارات `/cash/history` و`/cash` وعمليات التحويل والمصارفة الحالية متوافقة، وأضيفت المسارات الجديدة بصلاحيات القراءة القائمة: `cash.view` للصندوق و`wallets.transactions.view` للمحافظ. لا تعرض الواجهة أرصدة المحافظ لمن لا يملك صلاحية الرصيد؛ سجل الحركة نفسه يبقى محميًا بصلاحية سجل الحركات.

نجح اختبار `test_wallet_cash_ledgers.py`، وفحص صياغة Worker بصيغة ESM، وفحص 24 كتلة JavaScript مضمنة، ومجموعة الاختبارات الكاملة السابقة بما فيها اختبارات التحويلات والمصارفة واتزان القيود.


## تحديث العمل دون اتصال والمزامنة الدائمة — 26 أغسطس 2026

تمت إضافة طبقة `offline-sw-core.js` المشتركة إلى Service Worker الرئيسي والهاتف والمندوب. يتم تخزين طلبات API الكتابية التي تفشل بسبب انقطاع الشبكة في IndexedDB باسم `IbnMukhtarOfflineQueueDB` داخل مخزن دائم، ولذلك لا تختفي عند إغلاق التبويب أو المتصفح. يحتفظ كل طلب بالمسار والطريقة والرؤوس والجسم ومعرف العملية ومحاولات الإعادة وآخر خطأ وموعد المحاولة التالية.

عند عودة الاتصال، تُطلب المزامنة تلقائيًا من الصفحة، ويُسجل Background Sync بالوسم `pos-offline-sync` في المتصفحات التي تدعمه. توجد آلية بديلة عبر حدث `online` ومحاولة دورية كل دقيقة عندما لا يدعم المتصفح Background Sync. تعرض كل صفحات HTML، وعددها 28، مؤشرًا لحالة الاتصال وعدد العمليات المعلقة، ويمكن الضغط عليه لطلب المزامنة يدويًا. يحتفظ Service Worker كذلك بكاش لطلبات GET الناجحة، مع مفتاح مرتبط بالجلسة، حتى يمكن عرض آخر بيانات تمت قراءتها دون اتصال.

تم استثناء تسجيل الدخول والخروج وطلب الجلسة من الطابور؛ فلا تُحفظ محاولات المصادقة كعمليات مالية. كما أضيف جدول `offline_idempotency` إلى تهيئة Worker، ويُحفظ فيه معرف العملية ونتيجة الطلب الناجح. عند إعادة إرسال نفس العملية بسبب فقد الاستجابة، يعيد Worker النتيجة المحفوظة بدل إعادة تنفيذ القيد أو البيع أو التحويل. تُحذف السجلات الأقدم من 90 يومًا تدريجيًا.

يحافظ Service Worker المندوب على طابوره الخاص الحالي ومزامنته، بينما تستخدم الصفحات العامة والهاتف الطابور المشترك. جرى توسيع كاش الملفات ليشمل صفحات HTML وملفات JavaScript وCSS المشتركة، مع إبقاء كاش بيانات API عند تحديث Service Worker بدل حذفه تلقائيًا.

نجحت اختبارات الصياغة لكل Service Workers، واختبار `test_offline_sync.py` الذي تحقق من تغطية الصفحات الثماني والعشرين والطابور الدائم وBackground Sync وidempotency، إضافة إلى مجموعة الاختبارات الكاملة السابقة. نجح Wrangler dry-run بعد هذه التعديلات بحجم رفع **882.88 KiB** وgzip **149.21 KiB**، مع `No bindings found`، ودون نشر فعلي.

### حدود مهمة

المزامنة الخلفية تعتمد على دعم المتصفح لنظام Background Sync وسياسات النظام لتشغيل Service Worker؛ البديل الموجود يضمن الاستئناف عند فتح الصفحة أو عودة الاتصال، لكنه لا يستطيع إجبار نظام التشغيل على تشغيل المتصفح إذا أوقفه بالكامل أو منع تشغيل Service Worker. يجب أن يفتح المستخدم النظام مرة واحدة متصلًا حتى يُثبت Service Worker ويُخزن الكاش الأولي، ويجب اختبار التخزين المحلي على الجهاز المستهدف قبل الاعتماد.

تخزين GET محليًا قد يحفظ بيانات حساسة على الجهاز، لذلك يجب استخدام جهاز موثوق وتسجيل الخروج عند مشاركة الجهاز. لا ينبغي اعتبار العملية التي تظهر «معلقة» في المؤشر مكتملة ماليًا حتى تظهر حالتها «تمت المزامنة» أو يتم تأكيدها من سجل الخادم.


## إصلاح الجلسة عند انقطاع الإنترنت — 26 أغسطس 2026

تم تعديل `permissions.js` بحيث لا يؤدي فشل طلب `/auth/me` أو استجابة 503 الناتجة عن انقطاع الشبكة إلى حذف `pos_token` أو `pos_user` أو إعادة توجيه المستخدم إلى شاشة الدخول. أصبحت الجلسة تُمسح فقط عند استجابة 401/403 حقيقية من الخادم، أو عند ضغط المستخدم على زر تسجيل الخروج الذي يستدعي الحذف صراحةً.

تم تطبيق السلوك نفسه على `phone/index.html` و`phone/orders.html`. عند وجود جلسة محفوظة، يعرض تطبيق الهاتف بيانات المستخدم ويواصل فتح التطبيق في وضع عدم الاتصال عند فشل الشبكة، بينما يعيد طلب تسجيل الدخول فقط عند انتهاء الجلسة فعليًا أو عدم وجود رمز محفوظ. طلبات المصادقة نفسها مستثناة من طابور العمليات المالية.

نجح `test_session_offline.py` في إثبات مسارات الحذف والاستبقاء، ونجح فحص 24 كتلة JavaScript مضمنة، إضافة إلى مجموعة الاختبارات الكاملة للمشروع. لم يتم النشر إلى Cloudflare ولم تُعدّل قاعدة بيانات Turso الإنتاجية.


نتيجة Wrangler dry-run الأخيرة بعد إصلاح الجلسة: **886.56 KiB** إجمالي الرفع و**149.83 KiB** gzip، مع `No bindings found`، ودون نشر فعلي.

SHA-256 لأرشيف إصلاح الجلسة: `bb9bb404fbd00b2a42e598828e5b1ceb4b1a9844e16f2c8f34fca28a568053d5`.


## إصلاح تحميل سجلات الصندوق والمحافظ والموردين — 26 أغسطس 2026

تم إصلاح سبب رسالة «فشل تحميل حالة الصندوق» عند فتح `cash-wallets.html`: كانت `loadCashStatus` تستدعي دالة `loadCashHistory` غير موجودة، مما كان يلقي استثناءً بعد تجهيز حالة الصندوق. أصبحت تستدعي `loadCashLedger` مباشرة، مع حماية جلب المحافظ حتى لا يؤدي تعذر بيانات المحافظ إلى إخفاء حالة الصندوق.

أصبح سجل الصندوق وسجل المحافظ يُحمّلان تلقائيًا عند فتح الصفحة، وليس فقط بعد الضغط على زر التصفية. كما أضيف fallback إلى `/cash/history` إذا كان Worker المنشور قديمًا ولا يدعم `/cash/transactions`، وأضيفت معالجة آمنة للاستجابات الجزئية.

تم تحميل `filter-utils.js` في صفحة الصندوق والمحافظ، وربط الصفحة بالفلتر الموحد الموجود في الصفحات الأخرى. يتيح الفلتر الموحد تحديد الفترة من وإلى، اختيار اليوم أو الشهر أو السنة أو فترة مخصصة أو كل الفترات، حفظ الفترة، إظهار الفلتر وإخفاؤه، وتطبيق نفس الفترة على سجلي الصندوق والمحافظ. كما أضيفت أزرار مستقلة لإظهار وإخفاء الفلاتر التفصيلية لكل سجل.

تم تعريف الدالة العامة `clean` في رأس `suppliers.html`، بحيث أصبحت متاحة لدوال تفاصيل مرتجعات الموردين وقسم المنتجات المتبقية، مع استمرار حماية النصوص من HTML غير الموثوق. وبذلك عولج خطأ `clean is not defined` في الموضعين.

نجحت مجموعة الاختبارات الكاملة، واختبار إصلاحات واجهة الصندوق والموردين، وفحص 25 كتلة JavaScript مضمنة. لم يتم نشر Cloudflare ولم تُعدّل Turso الإنتاجية.


## تدقيق الجودة الشامل: offline والإشعارات والطباعة واستهلاك قاعدة البيانات — 26 أغسطس 2026

تمت مراجعة صفحات النظام وعددها 28 صفحة HTML. جميع الصفحات تحمل `offline-sync.js` و`number-utils.js` عند الحاجة. أضيف قفل in-flight داخل `offline-sw-core.js` لمنع تشغيل مزامنتين متوازيتين، وأضيف قفل مماثل لطابور المندوب في `driver/sw_driver.js`. كما أصبحت الطلبات الكتابية تحصل على `X-Offline-Operation-Id` قبل الإرسال، ويُمرر معرف عملية المندوب في رأس الطلب، ليتوافق مع جدول `offline_idempotency` في Worker ويمنع إعادة تنفيذ العملية عند إعادة المحاولة بعد فقد الاستجابة.

تم إصلاح Service Worker الهاتف بإبقاء استيراد `offline-sw-core.js` مرة واحدة فقط. وأصبح كاش الموارد الخارجية يقبل الاستجابات opaque عند دعمها، مع fallback آمن عند عدم توفر المورد، بينما تبقى بيانات GET المحلية مرتبطة بمفتاح جلسة المستخدم. لا تزال الموارد الخارجية التي لم تُفتح سابقًا بحاجة إلى تحميل متصل أول مرة؛ عند غيابها قد تتراجع الخطوط أو بعض الميزات الاختيارية، دون أن يؤدي ذلك إلى تسجيل خروج أو تنفيذ مالي ناقص.

تم تحسين إشعارات الإدارة بحيث لا تُطلب صلاحية Notification تلقائيًا عند فتح الصفحة؛ تظهر دعوة تفعيل من زر يضغطه المستخدم، ويتم تسجيل توكن FCM مرة واحدة لكل مستخدم وتوكن. كما تم منع طلبات التسجيل من المستخدمين غير الإداريين، وحماية إشعار المقدمة عند عدم دعم Notification. وفي صفحة اختبار الإشعارات أصبح اختبار المشرف يستخدم `pos_token`، واختبار المندوب يستخدم `driverToken`، وأزيل polling كل 30 ثانية واستُبدل بتحديث يدوي لتقليل استهلاك قاعدة البيانات. وصُحح إرسال JWT الخاص بالمندوب ليُرسل كما هو.

تم تعديل محرك الطباعة المركزي وطباعة فاتورة البيع وطباعة الباركود وطباعة فاتورة الهاتف لتفتح معاينة مستقلة تحتوي زرَي «طباعة» و«إغلاق»، دون استدعاء مربع الطباعة تلقائيًا أو انتظار `afterprint`. وبذلك تبقى الصفحة الأصلية قابلة للاستخدام حتى لو ترك المستخدم نافذة المعاينة أو مربع الطباعة مفتوحًا. fallback الطباعة الذي كان يستدعي `window.print()` داخل الصفحة أُزيل من مسارات الطلبات والمنتجات، ويظهر بدلًا منه تنبيه غير حاجب عند غياب محرك المعاينة.

لخفض استهلاك قاعدة البيانات، أصبح تحديث `orders.html` كل 60 ثانية بدل 30 ثانية، ويتوقف عندما تكون الصفحة مخفية أو غير متصلة. المؤقت الخاص بالمزامنة لا يرسل طلبًا جديدًا إذا كانت مزامنة سابقة قيد التنفيذ. أما استعلامات التفاصيل غير المحدودة التي ظهرت في الفحص الساكن فهي استعلامات مرتبطة بمعرّف فاتورة أو طلب أو مجموعة IDs محددة، وليست قوائم عامة؛ القوائم العامة تستخدم pagination. تبقى جداول tokens وبيانات التفاصيل بحاجة إلى مراقبة حجمها دوريًا في الإنتاج.

نجح اختبار الجودة النهائي `test_quality_offline_print_notifications.py`، واختبار الجلسة دون اتصال، واختبار offline-first، واختبارات السجلات والسندات والمحاسبة والتحويلات والمصارفة والمجموعات والتكامل. نجحت فحوص صياغة Worker وService Workers و25 كتلة JavaScript مضمنة. الفحص الساكن أكد تغطية 28 صفحة وعدم وجود صفحة بلا `offline-sync.js` أو `number-utils.js` عند الحاجة.

لم يتم نشر Cloudflare، ولم تُنفذ عمليات مالية، ولم تُعدّل Turso الإنتاجية. يلزم بعد النشر اختبار قبول على متصفح Chrome وSafari على الجهاز المستهدف، مع التحقق من Background Sync وFCM والصلاحيات وسياسة الكاش، وإجراء مصالحة قراءة فقط للأرصدة الفعلية. هذه المراجعة تقنية وليست اعتمادًا من محاسب قانوني أو مدقق مستقل.


### نتيجة dry-run النهائية

بعد التعديلات الأخيرة نجح `npx wrangler deploy --dry-run --outdir=/tmp/pos-wrangler-final` دون نشر: **886.56 KiB** إجمالي الرفع و**149.83 KiB** gzip، مع `No bindings found`. لم يتم تنفيذ أي طلب نشر أو تعديل على Turso.
