# تقرير مراجعة الصفحات والميزات المتبقية

## الملخص التنفيذي

نعم، ما زالت توجد صفحات تحتاج إلى مراجعة أو إضافة ميزات جديدة، لكن مستوى الحاجة يختلف. الصفحات التي طُوّرت في الجولة السابقة أصبحت أفضل من ناحية الواجهة والربط الأساسي، بينما تتركز الفجوات الحالية في **البيع والمشتريات والمنتجات والعملاء والموردين والطلبات والمندوب والإعدادات**. كما توجد مجموعة من وظائف `worker.js` موجودة في الخلفية ولا تملك واجهة مستخدم كاملة حتى الآن.

> النتيجة العملية: لا توجد حاجة لإعادة بناء كل الصفحات من الصفر. الأفضل تنفيذ جولة ثالثة مركزة على إكمال الميزات الخلفية غير المستعملة، ثم توحيد الجداول والتصدير والتصفية والصلاحيات بين الصفحات.

## نطاق المراجعة

تم فحص الصفحات الموجودة في النسخة الحالية، وعددها 22 صفحة HTML، مع مراجعة `worker.js` وملفات Service Worker وملفات التنقل. شملت المراجعة الصفحات التي لم تدخل في دفعة التطوير السابقة، وهي البيع، المشتريات، المنتجات، العملاء، الموردون، الطلبات، الطلب الإلكتروني، التقارير الإلكترونية، صفحة المندوب، الإعدادات، لوحة التحكم، اختبار الإشعارات، وطباعة الباركود.

| المجموعة | الصفحات |
|---|---|
| المبيعات والمشتريات | `sale.html`، `purchases.html`، `invoices.html`، `operations.html` |
| المخزون والمنتجات | `products.html`، `stock-movements.html`، `barcode-print.html` |
| الحسابات | `customers.html`، `suppliers.html`، `cash-wallets.html`، `payments.html`، `expenses.html`، `journal.html`، `reports.html` |
| الطلبات والتوصيل | `add_order_ph.html`، `orders.html`، `online-reports.html`، `driver/index.html` |
| الإدارة | `index.html`، `settings.html`، `notification-test.html` |

## التصنيف حسب الأولوية

| الأولوية | الصفحات أو الملفات | سبب الأولوية | نوع العمل المقترح |
|---|---|---|---|
| عاجلة | `orders.html`، `driver/index.html`، `add_order_ph.html` | دورة الطلب والتوصيل تعتمد على حالات وتزامن وإشعارات، وأي خلل فيها يؤثر على التشغيل مباشرة | إكمال تحديث الحالات، عناصر الطلب، الإرجاعات، التزامن دون اتصال، وسجل العمليات |
| عالية | `sale.html`، `purchases.html`، `products.html` | هي قلب المخزون والمبيعات، وتحتاج إلى توحيد التصدير والترقيم والإرجاع والربط بالمخزون | إضافة التصدير، pagination، سجل التغييرات، وواجهات الإرجاع والعكس |
| عالية | `customers.html`، `suppliers.html` | الصفحتان تحتويان على أرصدة ومدفوعات وكشوف، لكن توجد وظائف Worker مالية غير مستعملة | إضافة إجمالي الدين، تاريخ الشراء، التصدير، والتحقق المالي قبل الإلغاء |
| متوسطة | `settings.html`، `index.html` | تحتاج إلى تحسين الإدارة والصلاحيات والتعامل مع النسخ الاحتياطي والذكاء الاصطناعي | صلاحيات، تأكيدات، فحص الاستيراد، مؤشرات حالة التكاملات |
| متوسطة | `online-reports.html`، `barcode-print.html` | الصفحتان تعملان وظيفيًا، لكن يمكن تحسين التحليل والتصدير والطباعة الجماعية | فلاتر متقدمة، CSV، تقارير حسب الحالة، وقوالب باركود محفوظة |
| منخفضة | `notification-test.html`، `add_order.html` | صفحة الاختبار أداة إدارية وليست جزءًا من التشغيل اليومي، و`add_order.html` صفحة تحويل مختصرة | توحيد التسمية، حماية الصلاحيات، وتحسين رسائل الاستخدام |

## نتائج الصفحات الأساسية

### 1. صفحة البيع `sale.html`

الصفحة قوية من ناحية تصميم سلة البيع، اختيار العميل، العملات، الدفع النقدي والمحافظ والدفع المختلط، حساب الخصم، وإظهار التكلفة والربح. لكنها لا تقدم داخل الصفحة نفسها وظيفة واضحة للإرجاع الكامل أو تصدير قائمة المبيعات، وتعتمد على صفحات أخرى لتنفيذ بعض العمليات.

التطوير المقترح هو إضافة **حفظ مسودة البيع**، وإظهار تنبيه واضح عند وجود دين على العميل، وإضافة زر للوصول إلى سجل العميل والفواتير السابقة، وإضافة طباعة موحدة بعد الحفظ، مع جعل التحقق من المخزون والدفع قبل الإرسال أكثر وضوحًا. كما يستحسن إضافة تصدير سريع للفواتير الأخيرة بدل الاعتماد على `invoices.html` فقط.

**التقييم:** واجهة قوية، لكنها تحتاج إلى استكمال دورة ما بعد البيع وربطها بسجل العميل والمخزون.

### 2. صفحة المشتريات `purchases.html`

الصفحة تحتوي على إدارة المورد، العملات، المحافظ، أصناف الفاتورة، الإرجاع والإلغاء، لكنها لا تظهر فيها آلية pagination وتصدير مماثلة لما تم تطبيقه في صفحة الفواتير والسندات. كثرة الاعتماد على قائمة المنتجات والموردين الكبيرة قد تؤثر على السرعة عند نمو البيانات.

التطوير المقترح هو إضافة pagination حقيقي، بحث خادمي، تصدير CSV، فلتر حسب المورد والحالة والتاريخ، إظهار تكلفة الشراء بعد الخصم والشحن، وإضافة سجل تعديلات الفاتورة. كما ينبغي إضافة زر للوصول إلى حركات المخزون الناتجة عن فاتورة الشراء.

**التقييم:** وظيفية، لكنها تحتاج تحسينًا تشغيليًا وإحصائيًا.

### 3. صفحة المنتجات `products.html`

الصفحة متقدمة نسبيًا وتدعم التصنيفات والموردين والمخزون والتعديل والتكرار وتوليد SKU. إلا أن `worker.js` يحتوي على مجموعة وظائف لمنتجات معلقة أو غير مرتبطة لا تملك واجهة مكتملة في الصفحة، ومنها المنتجات غير المرتبطة، ربط المنتج، إلغاء الربط، تحديث المخزون، وتأكيد نشر المنتج.

التطوير المقترح هو إضافة تبويب **مراجعة المنتجات المعلقة**، وتبويب **المنتجات غير المرتبطة**، مع إجراءات الربط والإلغاء والتخطي وتحديث المخزون. كما يفضل إضافة استيراد منتجات جماعي مع معاينة الأخطاء وسجل آخر تعديل.

**التقييم:** من أقوى الصفحات الحالية، لكن فجوة ربط المنتجات مع الـ Worker مهمة.

### 4. صفحة العملاء `customers.html`

الصفحة تدعم كشف الحساب والمدفوعات والمبيعات وبعض عمليات الإلغاء، لكنها لا تستخدم وظائف Worker الخاصة بإجمالي الدين وتاريخ الشراء بشكل مستقل. كما أن التصدير والتقارير الفردية للعميل يمكن تطويرها أكثر.

التطوير المقترح هو إضافة بطاقة إجمالي الدين، تاريخ شراء العميل، متوسط قيمة الفاتورة، آخر سداد، تصدير كشف العميل إلى CSV، وإضافة زر إرسال أو طباعة كشف حساب مختصر. وينبغي منع الإلغاء المالي دون عرض أثر العملية على الرصيد والقيود.

**التقييم:** جيدة وظيفيًا، وتحتاج طبقة تحليل مالي وإخراج تقارير أفضل.

### 5. صفحة الموردين `suppliers.html`

الصفحة تحتوي على تفاصيل المورد، فواتير الشراء، المدفوعات، كشف الحساب، الإرجاعات، والمخزون المتبقي. لكنها لا تستفيد من مسارات `total-debt` و`financial-balance` الموجودة في Worker، كما أن التصدير الموحد غير ظاهر كميزة أساسية.

التطوير المقترح هو إضافة ملخص مالي للمورد، إجمالي المشتريات، إجمالي المدفوع، المتبقي، قيمة المرتجعات، والمخزون المتبقي حسب المنتج. كما ينبغي إضافة تصدير كشف المورد وتقرير حركة صنف المورد.

**التقييم:** جيدة ومتكاملة نسبيًا، وتحتاج تقارير مالية مختصرة وتصديرًا.

## نتائج صفحات الطلبات والمندوب

### 6. صفحة الطلبات `orders.html`

الصفحة تدعم عرض الطلبات، إلغاء الطلب، تعيين المندوب، ملخص المندوب، الدفعات، الإرجاعات، وإرجاع عناصر الطلب. لكن وظيفة تحديث الحالة العامة للطلب الموجودة في Worker، وهي `/online-orders/update-status`، لا تظهر كوظيفة واضحة مستخدمة في الصفحة. كما أن المسار `/driver/order-items` لا يظهر له استخدام مباشر، مما قد يجعل تفاصيل عناصر الطلب موزعة بين أكثر من مسار.

التطوير المقترح هو بناء شريط حالات موحد للطلب: جديد، قيد التجهيز، جاهز، مع المندوب، تم التسليم، فشل التسليم، مرتجع، ملغى. ويجب إضافة سجل زمني لكل انتقال، وتأكيد قبل تغيير الحالة، وتحديث فوري للمندوب والإدارة، مع فلتر حسب الحالة والمندوب والمنطقة والتاريخ.

**التقييم:** أولوية عالية، لأن الصفحة تمثل مركز إدارة دورة الطلب.

### 7. صفحة إضافة الطلب `add_order_ph.html`

الصفحة الفعلية كبيرة وتحتوي على وظائف متعددة: المنتجات والعملاء والمحافظ والمندوبين والطلبات والتعديل والإلغاء. هذا جيد من ناحية الوظائف، لكنه يجعل الصفحة صعبة الصيانة والاختبار، كما أن حجمها الكبير يزيد احتمالية تداخل الحالات المحلية مع حالات Worker.

التطوير المقترح هو تقسيمها إلى وحدات واضحة: بيانات العميل، عناصر الطلب، الدفع، التوصيل، والملاحظات. ويجب إضافة حفظ مسودة، منع التكرار عند الضغط المتكرر، سجل محاولات المزامنة، وإظهار رقم العملية المحلية عند العمل دون اتصال.

**التقييم:** غنية وظيفيًا، لكن تحتاج تنظيمًا داخليًا وتحسينًا للتزامن.

### 8. صفحة المندوب `driver/index.html`

الصفحة تحتوي على وظائف متقدمة جدًا للتخزين المحلي والتزامن والإشعارات وتحديث التسليم والإرجاعات. وجود عبارة تشغيلية تفيد بأن العملية ستبقى للمزامنة اللاحقة يعني أن التزامن دون اتصال جزء أساسي من النظام، ولذلك تحتاج الصفحة إلى اختبارات حالات فشل حقيقية، منع تكرار العملية، وإظهار قائمة العمليات المعلقة للمستخدم.

التطوير المقترح هو إضافة مركز مزامنة واضح، رقم عملية لكل تحديث، حالة آخر مزامنة، إعادة المحاولة اليدوية، سجل الأخطاء، وتأكيد بصري عند وصول الطلب إلى الخادم. كما يجب توحيد صفحة تفاصيل الطلب مع `/driver/order-items` وفتح رابط الإشعار على المسار الصحيح دائمًا.

**التقييم:** أولوية عاجلة، بسبب حساسية التوصيل والعمل دون اتصال.

### 9. التقارير الإلكترونية `online-reports.html`

الصفحة مناسبة للعرض والتحليل الأساسي للطلبات الإلكترونية، وتحتوي على وظائف تصدير، لكنها تحتاج إلى جعل التقارير أكثر فائدة للإدارة، عبر المقارنة بين الحالات، معدل الإلغاء، معدل فشل التسليم، أداء المندوبين، ومتوسط زمن الطلب.

**التقييم:** تعمل، لكنها تحتاج طبقة مؤشرات وتحليلات إدارية.

## نتائج صفحات الإدارة

### 10. لوحة التحكم `index.html`

لوحة التحكم تجمع مبيعات ومنتجات وعملاء ومحافظ وصندوق، وتحتوي على تكامل الذكاء الاصطناعي. ينبغي إضافة مؤشرات صريحة لحالة الاتصال، آخر تحديث، مصدر الأرقام، ومتى فشل أحد الطلبات الخلفية. كما ينبغي ألا يظهر زر الذكاء الاصطناعي كأنه مضمون العمل إذا لم يتم ضبط متغيرات البيئة الخاصة به في Worker.

التطوير المقترح هو إضافة بطاقات الديون، المبيعات الآجلة، المصروفات، الطلبات المتأخرة، المنتجات منخفضة المخزون، وأخطاء المزامنة. كما ينبغي إخفاء أو تعطيل أدوات الإدارة حسب دور المستخدم.

### 11. الإعدادات `settings.html`

الصفحة تدعم الإعدادات، توليد SKU، النسخ الاحتياطي والاستعادة، أرقام الفواتير، والمستخدمين. لكنها تحتاج إلى فصل واضح بين الإعدادات العامة وإدارة المستخدمين والنسخ الاحتياطي، مع تحذير قبل الاستعادة وفحص بنية الملف قبل الإرسال.

التطوير المقترح هو إضافة صلاحيات حسب الدور، سجل تغييرات الإعدادات، معاينة النسخة الاحتياطية، فحص حجم الملف ونوعه، منع الاستعادة العرضية، ومؤشر نجاح أو فشل كل قسم بدل رسالة عامة.

### 12. اختبار الإشعارات `notification-test.html`

هذه الصفحة مناسبة للاختبار الداخلي، لكنها يجب أن تكون محمية بصلاحية إدارية واضحة، وأن تعرض عدد الرموز المسجلة، آخر إرسال، نتيجة كل جهاز، وفشل الإرسال بالتفصيل. لا ينبغي اعتبارها صفحة تشغيل يومية.

### 13. طباعة الباركود `barcode-print.html`

الصفحة تعمل للطباعة من المنتجات أو فواتير الشراء، وتتيح ضبط المقاسات وعدد الصفوف والأعمدة. الإضافة المفيدة هي حفظ قوالب متعددة، معاينة عدد الملصقات قبل الطباعة، دعم استيراد قائمة باركود، وإضافة زر طباعة مباشرة من صفحة المنتجات.

## وظائف Worker الموجودة دون واجهة مكتملة

| وظيفة Worker | الحالة الحالية | الواجهة المقترحة |
|---|---|---|
| `/sales/return/undo` | لا توجد واجهة واضحة | زر عكس الإرجاع داخل سجل العمليات مع صلاحية مدير |
| `/sales/cancel/undo` | لا توجد واجهة واضحة | زر استعادة الفاتورة الملغاة مع سجل تدقيق |
| `/purchases/cancel/undo` | لا توجد واجهة واضحة | زر عكس إلغاء فاتورة الشراء |
| `/customers/total-debt` | غير مستخدمة كواجهة مستقلة | بطاقة دين العميل وتقرير أعمار الديون |
| `/customers/purchase-history` | غير مستخدمة كواجهة مستقلة | تبويب تاريخ شراء العميل |
| `/suppliers/total-debt` | غير مستخدمة كواجهة مستقلة | ملخص دين المورد |
| `/suppliers/financial-balance` | غير مستخدمة كواجهة مستقلة | تقرير مالي للمورد |
| `/cash/history` | لا تظهر كوظيفة مستقلة | سجل صندوق مع فلترة وتصدير |
| `/api/pending-products` | لا توجد واجهة مكتملة | تبويب المنتجات المعلقة |
| `/api/confirm-product-publish` | لا توجد واجهة مكتملة | تأكيد نشر المنتج |
| `/api/link-product` | لا توجد واجهة مكتملة | شاشة ربط المنتجات |
| `/api/skip-pending-product` | لا توجد واجهة مكتملة | تخطي المنتج المعلق مع سبب |
| `/api/pos-products-unlinked` | لا توجد واجهة مكتملة | تقرير المنتجات غير المرتبطة |
| `/api/pos-products-linked` | لا توجد واجهة مكتملة | تقرير المنتجات المرتبطة |
| `/api/update-stock` | لا توجد واجهة مستقلة | تحديث مخزون يدوي مع سجل سبب |
| `/api/unlink-product` | لا توجد واجهة مستقلة | إلغاء ربط المنتج |
| `/online-customers/by-phone` | لا تظهر كوظيفة مستقلة | بحث عميل الطلب برقم الهاتف |
| `/online-orders/update-status` | لا تظهر كوظيفة واضحة في الطلبات | شريط حالات الطلب وسجل زمني |
| `/driver/order-items` | لا تظهر كواجهة مستقلة | تفاصيل أصناف الطلب في تطبيق المندوب |
| `/reports/*-by-category` | مرتبطة بأزرار ديناميكية في `reports.html` | تحتاج فقط تحسين العرض والتصدير، وليست غائبة فعليًا |

## ترتيب التطوير المقترح

### المرحلة الأولى: دورة الطلب والتوصيل

يبدأ العمل بـ `orders.html` و`add_order_ph.html` و`driver/index.html`. الهدف هو توحيد حالات الطلب، ربط تفاصيل العناصر، تحسين الإرجاع، وتسجيل التزامن دون اتصال. هذه المرحلة ذات أعلى أثر تشغيلي.

### المرحلة الثانية: البيع والمشتريات والمخزون

بعد تثبيت الطلبات، يتم تطوير `sale.html` و`purchases.html` و`products.html` لإضافة المسودات، التصدير، pagination، تحديث المخزون اليدوي، وربط المنتجات المعلقة.

### المرحلة الثالثة: الحسابات والعملاء والموردون

تتضمن إضافة إجمالي الديون، تاريخ الشراء، التقارير الفردية، تصدير الكشوف، وعكس العمليات بصلاحيات واضحة في `customers.html` و`suppliers.html` و`cash-wallets.html`.

### المرحلة الرابعة: الإدارة والتقارير

تتضمن تطوير `index.html` و`settings.html` و`online-reports.html` و`notification-test.html` بإضافة مؤشرات الاتصال، الصلاحيات، النسخ الاحتياطي الآمن، تحليلات الطلبات، وسجل الإشعارات.

## الخلاصة

الصفحات التي تحتاج مراجعة فعلية ليست كل الصفحات بنفس الدرجة. **الأولوية العاجلة هي الطلبات والمندوب والتزامن دون اتصال**. بعدها تأتي **المبيعات والمشتريات وربط المنتجات والمخزون**، ثم **الديون وتقارير العملاء والموردين**. أما صفحات السندات والمصروفات والقيود والتقارير والعمليات والفواتير فقد حصلت على تطوير جيد، وتحتاج لاحقًا إلى تحسينات تكاملية أكثر من حاجتها إلى إعادة بناء.

أوصي بأن تكون الخطوة التالية تطوير `orders.html` أولًا، ثم ربط التعديلات مع `driver/index.html` و`add_order_ph.html`، لأن هذه الصفحات تشكل دورة عمل واحدة ولا يصح تطوير كل واحدة بمعزل عن الأخرى.
