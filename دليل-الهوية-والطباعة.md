# دليل الهوية البصرية والطباعة الموحدة

## 1. الملفات التي تم تحديثها

الملف المركزي هو `document-utils.js`. يجب تحميله في كل صفحة تحتاج الطباعة أو التصدير قبل استخدام أزرار المستندات. تمت إضافة الحقول إلى `settings.html`، وتمت إضافة القيم الافتراضية والتحقق الخادمي في `worker.js`.

| الحقل | الوظيفة |
|---|---|
| `shop_name` | اسم المنشأة |
| `shop_phone` | رقم الهاتف |
| `shop_address` | عنوان المنشأة |
| `shop_logo` | رابط صورة أو Data URL للشعار |
| `brand_primary` | اللون الأساسي |
| `brand_secondary` | اللون الثانوي |
| `brand_text` | لون النص |
| `invoice_footer` | العبارة أسفل الفاتورة أو السند |

## 2. ضبط الهوية من صفحة الإعدادات

افتح `settings.html` بحساب المدير، ثم أدخل اسم المنشأة والهاتف والعنوان. يمكن وضع رابط مباشر لصورة الشعار، أو اختيار ملف PNG أو JPG أو SVG من الجهاز. عند اختيار ملف من الجهاز يحوله المتصفح إلى Data URL ويحفظه في `shop_logo`.

يفضل استخدام شعار PNG بخلفية شفافة وبحجم لا يتجاوز 1MB. إذا استُخدم رابط خارجي، يجب أن يكون الرابط ثابتًا وأن يسمح خادم الصور بالوصول من المتصفح. أما Data URL فيتجنب مشكلة CORS، لكنه يزيد حجم قيمة الإعداد في قاعدة البيانات.

اختر الألوان من حقول الألوان، ثم أدخل عبارة التذييل. عند الضغط على حفظ، ترسل الصفحة الحقول إلى:

```text
POST /settings
```

ويحفظ Worker كل قيمة في جدول `settings` باستخدام المفتاح والقيمة. لا توجد حاجة لإنشاء جدول جديد للهوية؛ تستخدم الهوية جدول الإعدادات الحالي.

## 3. استخدام المحرك في أي صفحة

أضف السطر التالي قبل إغلاق `body`:

```html
<script src="document-utils.js"></script>
```

بعد تحميل الملف تصبح الدوال التالية متاحة:

```javascript
POSDocs.getIdentity();
POSDocs.setPaper('a4');
POSDocs.setPaper('80mm');

POSDocs.printInvoiceData({
  type: 'sale',
  number: 'INV-1001',
  date: new Date().toLocaleString(),
  party: 'عميل نقدي',
  payment_method: 'نقدي',
  items: [
    { name: 'منتج تجريبي', quantity: 2, unit_price: 150, line_total: 300 }
  ],
  total: 300,
  paid: 300
}, { title: 'فاتورة مبيعات' });

POSDocs.exportInvoicePDF({
  type: 'purchase',
  number: 'PUR-1001',
  party: 'المورد',
  items: [{ name: 'منتج', quantity: 5, unit_price: 20, line_total: 100 }],
  total: 100
}, { filename: 'فاتورة-مشتريات.pdf' });
```

ولطباعة سند:

```javascript
POSDocs.printVoucherData({
  id: 25,
  type: 'receipt',
  amount: 500,
  currency_code: 'YER',
  reason: 'دفعة من العميل',
  date: new Date().toLocaleString()
});
```

يدعم المحرك أيضًا `POSDocs.renderInvoice(data)` و`POSDocs.renderVoucher(data)` إذا احتجت وضع المستند داخل نافذة مخصصة.

## 4. إعدادات طباعة PDF

للحصول على نتيجة صحيحة في PDF، اختر المقاس من شريط **المستندات الموحدة** قبل الضغط على PDF. استخدم **A4** للفواتير والكشوفات والتقارير، واستخدم **80mm** للطابعات الحرارية ذات عرض 80mm. يحفظ المحرك المقاس المختار في المتصفح، لذلك سيبقى الاختيار نفسه عند فتح صفحة أخرى على الجهاز نفسه.

| الإعداد | A4 | 80mm |
|---|---:|---:|
| مقاس الورق | A4 | 80mm Roll |
| الهوامش | يفضل Default أو None حسب تعريف الطابعة | None أو أقل هامش متاح |
| المقياس | 100% | 100% أو Actual size |
| الرسومات الخلفية | مفعلة | مفعلة |
| الاتجاه | Portrait | Portrait |
| احتواء الصفحة | غير مفضل إذا أدى إلى تصغير النص | يستخدم فقط إذا تجاوزت الفاتورة العرض |

عند استخدام أمر طباعة المتصفح، فعّل خيار **Background graphics** أو **Print backgrounds** حتى تظهر خلفيات العناوين والألوان. لا تستخدم خيار **Headers and footers** في إعدادات المتصفح حتى لا يضيف المتصفح رابط الصفحة والتاريخ خارج قالب الفاتورة.

ألوان الهوية تستخدم داخل CSS وداخل خيارات PDF، لكن ظهورها على الورق يعتمد أيضًا على إعداد الطابعة. في الطباعة الفعلية عطّل وضع **Black and white** أو **Draft/Economy**، واختر جودة Normal أو High. أما PDF الناتج من `html2pdf.js` فيحافظ على ألوان CSS غالبًا، مع ضرورة أن يكون الشعار قابلًا للتحميل.

## 5. مشكلة عدم ظهور الشعار

إذا ظهر الشعار في الشاشة ولم يظهر في PDF، استخدم أحد الحلول التالية: احفظ الشعار كـ Data URL من حقل رفع الشعار، أو استضف الصورة على نطاق يسمح بـ CORS، أو استخدم PNG بدل صورة تعتمد على تحويلات SVG خارجية. يجب أن يكون الشعار متاحًا قبل إنشاء PDF، لأن المحرك يستخدم `useCORS` و`allowTaint:false` لتجنب الصور غير المسموح بها.

## 6. مكتبات التصدير

يحمّل المحرك المكتبات عند أول استخدام فقط:

```text
html2pdf.js  لإنشاء PDF
SheetJS      لإنشاء ملفات Excel بصيغة XLSX
```

لذلك يحتاج أول تصدير PDF أو Excel إلى اتصال بالإنترنت إذا لم تكن المكتبات محملة مسبقًا. بعد التحميل يحتفظ المتصفح بها عادة في الذاكرة حتى إعادة تحميل الصفحة.

## 7. فحص الإعدادات بعد النشر

بعد نشر Worker والواجهات، اختبر بالترتيب التالي: احفظ الهوية من `settings.html`، أعد فتح الإعدادات وتأكد من بقاء القيم، افتح فاتورة مبيعات واضغط PDF بمقاس A4، افتح فاتورة مشتريات واضغط PDF بمقاس 80mm، افتح سند قبض واضغط Excel، ثم افتح كشف عميل أو مورد واختبر الطباعة والتصدير.

إذا لم تظهر القيم في الصفحات، تحقق من أن عنوان API في الصفحات يشير إلى Worker الصحيح، وأن طلب `GET /settings` يعيد المفاتيح الجديدة، وأن الصفحة تحمل `document-utils.js` من المسار الصحيح.
