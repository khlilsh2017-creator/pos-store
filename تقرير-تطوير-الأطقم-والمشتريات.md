# تقرير تطوير الأطقم والمشتريات

## الملخص

تم تنفيذ الجولة المطلوبة على مشروع POS العربي مع الحفاظ على مجلد `driver/` دون تعديل. أصبح الطقم يعتمد على **أسماء المقاسات التي يحددها المستخدم**، ولكل مقاس مخزون مستقل وتكلفة موزعة وسعر بيع وباركود مستقل؛ وعند ترك السعر فارغًا يستخدم النظام السعر الأساسي للمنتج تلقائيًا. كما أصبح بالإمكان بيع الطقم كاملًا بسعره الإجمالي أو بيع مقاس واحد بسعره الخاص.

> لا يتم توزيع الرصيد القديم للطقم عشوائيًا على المقاسات. إذا كان الطقم قديمًا ولا يملك سجلات `product_variants` فعلية، يجب إعادة حفظ تعريف المقاسات من شاشة المنتجات ثم إدخال أرصدة المقاسات عبر المشتريات، وذلك لحماية دقة المخزون.

## سبب ظهور سعر أقل من التكلفة

في النسخة السابقة كانت بعض سجلات `product_variants` القديمة تحفظ تكلفة الطقم الكاملة، مثل `10,000`، داخل كل مقاس. لذلك كان النظام يقارن سعر `5,000` للمقاس الكبير مع `10,000` بدل مقارنته مع التكلفة الموزعة الصحيحة `2,000`. كما كان فحص الحفظ المحلي في أحد المسارات يقارن سعر المقاس بتكلفة الطقم الكاملة، وهو تحقق غير صحيح.

تم تصحيح ذلك في Worker والواجهة. أصبحت تكلفة المقاس تحسب كالتالي: `تكلفة الطقم ÷ عدد المقاسات`. وفي المثال المرسل تكون تكلفة كل مقاس `10,000 ÷ 5 = 2,000`، ولذلك فإن أسعار `5,000` و`4,500` و`3,500` و`3,000` و`3,000` كلها أعلى من تكلفة المقاس وليست مخالفة. عند تشغيل Worker بعد النشر تُصحح تكاليف المقاسات القديمة إلى التكلفة الموزعة، ويمنع النظام لاحقًا حفظ أي سعر مقاس أقل من هذه التكلفة.

## ما تم تنفيذه

| المجال | التنفيذ |
|---|---|
| قاعدة البيانات | إضافة جدول `product_variants` يحوي المنتج، اسم المقاس، مخزون المقاس، سعر البيع، التكلفة، الباركود والحالة، مع فهارس مناسبة وفهرس باركود فريد. |
| تعريف الطقم | إزالة الحاجة إلى اسم قطعة عام لكل صف. النموذج يطلب اسم المقاس الذي يحدده المستخدم، مع سعر بيع وباركود اختياريين لكل مقاس. |
| البيع | النقر على الطقم يفتح نافذة تختار بين بيع الطقم كاملًا أو بيع مقاس واحد. في بيع المقاس تظهر الأسماء والمخزون والأسعار، أما مسح باركود المقاس فيفتح المقاس المطابق مباشرة. |
| خصم المخزون | عند بيع مقاس منفرد يتم خصم `product_variants.stock_quantity` للمقاس المحدد فقط. وعند بيع الطقم كاملًا يتحقق النظام من توفر جميع المقاسات ثم يخصم كمية واحدة من كل مقاس في معاملة ذرية، ويُحدّث إجمالي مخزون المنتج الأب بعدد القطع المتأثرة. |
| الفاتورة | يسجل `sale_items.variant_id` و`variant_label`، وتظهر عبارة المقاس في السلة والإيصال/البيانات. كما تظهر في فواتير طلبات الإنترنت والتقارير وتفاصيل الطلب. |
| طلبات الإنترنت | شاشة `add_order_ph.html` تدعم اختيار بيع الطقم كاملًا أو مقاس واحد، وترسل `variant_id` و`variant_label` و`sale_mode`، ويخصم Worker مخزون المقاس أو جميع المقاسات حسب الوضع. |
| فلتر اليوم | تم توحيد قبول `from/to` و`date_from/date_to`، وتحويل حدود اليوم المحلي GMT+03 إلى حدود UTC قبل استعلام SQLite، مع إصلاح الطلبات الإلكترونية والتقرير اليومي وتقارير التصنيف وP&L والتصدير. |
| المشتريات | يمكن اختيار شراء الطقم كاملًا، فتزداد كمية جميع المقاسات وتُقسم تكلفة الطقم بالتساوي عليها، أو شراء مقاس منفرد فتزداد كمية المقاس المحدد فقط، مع تحديث الإجمالي الأب. |
| اسم المنتج | أصبح اسم المنتج في كل سطر شراء حقلًا قابلًا للتحرير، ويُحفظ في جدول المنتجات عند اعتماد الفاتورة. |
| سعر البيع | أصبح تعديل سعر البيع في سطر الشراء يُحفظ في `products.price` للمنتج العادي، أو في سعر المقاس للطقم عند تحديد مقاس. |
| حد التكلفة | الواجهة وWorker يرفضان سعر بيع أقل من تكلفة السطر، ويقارن سعر المقاس بتكلفته الموزعة لا بتكلفة الطقم الكاملة. |
| الباركود | بقي الباركود اختياريًا؛ عند تركه فارغًا يولد Worker باركودًا فريدًا للمنتج أو للمقاس. لكل مقاس باركود مستقل، ويمنع النظام تعارضه مع باركود المنتج الأب أو أي مقاس آخر. وتطبع صفحة الباركود اسم المقاس وسعره وباركوده. |
| driver | تمت المحافظة عليه دون أي تعديل، واجتاز فحص المقارنة مع الأرشيف. |

## طريقة الاستخدام

من شاشة **المنتجات** فعّل خيار «المنتج طقم / مجموعة»، ثم أدخل عدد المقاسات. اكتب في كل صف اسم المقاس فقط، مثل «صغير» أو «كبير» أو «24 سم». اترك سعر المقاس فارغًا إذا أردت استخدام السعر الأساسي تلقائيًا، أو أدخل سعرًا مستقلًا لذلك المقاس. لا تحتاج إلى إدخال أسماء القطع داخل الطقم.

بعد حفظ الطقم، اكتب أسماء المقاسات وأسعارها وباركوداتها الاختيارية من شاشة **المنتجات**. عند شراء طقم كامل بقيمة 10,000 وعدد خمسة مقاسات، يسجل النظام تكلفة 2,000 لكل مقاس ويزيد رصيد كل مقاس بكمية واحدة. افتح شاشة البيع واضغط على الطقم؛ ستظهر نافذة بها زر «بيع الطقم كاملًا» بسعر الطقم، وزر «بيع مقاس واحد» مع أسماء المقاسات وأسعارها ومخزونها. عند بيع مقاس منفرد يُخصم ذلك المقاس فقط، وعند بيع الطقم كاملًا تُخصم جميع المقاسات معًا. كما يؤدي مسح باركود مقاس إلى فتحه مباشرة بسعره المحدد.

في شاشة المشتريات أصبح الاسم قابلًا للتحرير مباشرة داخل الجدول. عند اختيار «كامل» للطقم يوزع النظام التكلفة على جميع المقاسات ويزيد مخزونها، وعند اختيار اسم مقاس يضيف الكمية لذلك المقاس فقط. عند تغيير التكلفة يرفع النظام سعر البيع إلى حد التكلفة الأدنى إذا أصبح السعر أقل منها، كما يرفض الاعتماد النهائي لأي سطر مخالف.

## ملاحظة على الأطقم القديمة

الأطقم التي كانت تستخدم `set_details_json` فقط تُعرض مقاساتها القديمة للمرجعية، لكن لا يُفترض أن النظام يعرف توزيع مخزونها السابق بين المقاسات. لذلك لا يسمح البيع منها حتى تُنشأ سجلات مقاسات فعلية. الإجراء الآمن هو فتح المنتج، مراجعة أسماء المقاسات، حفظه، ثم إدخال أرصدة كل مقاس من خلال المشتريات.

## الاختبارات المنفذة

تم تشغيل فحص JavaScript لـ `worker.js` والسكربتات المضمنة في `products.html` و`purchases.html` و`sale.html` و`barcode-print.html`. كما تم تشغيل اختبار SQLite وظيفي يثبت توزيع تكلفة طقم من خمس مقاسات، وبيع مقاس منفرد بسعره وخصم رصيده فقط، وبيع الطقم كاملًا وخصم جميع المقاسات ذريًا، وأن السعر الأقل من التكلفة مرفوض.

| مجموعة الاختبارات | النتيجة |
|---|---|
| `test_product_variants_sqlite.py` | ناجح: طقم من خمس مقاسات، توزيع تكلفة 10,000 إلى 2,000، بيع منفرد، بيع كامل، وحد التكلفة. |
| `test_product_sets.py` | ناجح: النموذج، endpoint المقاسات، السلة، البيع الكامل والمنفرد، الباركود والنحو. |
| `test_new_features.py` | ناجح. |
| `test_filter_routes.py` | ناجح. |
| `test_inline_print_syntax.py` | ناجح. |
| `test_print_party_names.py` | ناجح. |
| `test_category_hierarchy.py` | ناجح. |
| `test_department_pnl.py` | ناجح. |
| `test_site_integration.py` | ناجح. |
| صفحة `barcode-print.html` | ناجح: عرض وطباعة كل مقاس بباركوده وسعره. |
| `check_project.py` | ناجح. |
| `compare_driver_archive.py` | ناجح: driver unchanged. |
| دعم طلبات الإنترنت وفلتر اليوم | ناجح: syntax، مؤشرات الواجهة، حقول المقاس، وحدود التاريخ المحلية. |
| فحص تعريف `linkProductByCode` | ناجح: تعريف واحد فقط. |

## النشر والتحقق الإنتاجي

يتطلب التغيير نشر `worker.js` حتى تعمل migration الخاصة بجدول `product_variants` والأعمدة الجديدة في `sale_items` و`purchase_invoice_items` و`online_order_items`، ويجب تحديث صفحات `add_order_ph.html` و`orders.html` و`online-reports.html` و`invoices.html` و`filter-utils.js`. بعد النشر يجب تنفيذ اختبار صغير بحساب مدير موثق: إنشاء طقم بمقاسين، إدخال كمية مختلفة لكل مقاس، بيع مقاس واحد، ثم التأكد من أن رصيد المقاس الآخر لم يتغير وأن الإجمالي الأب انخفض بكمية البيع. كما يجب اختبار تغيير اسم وسعر منتج عادي من فاتورة شراء، ثم محاولة إدخال سعر بيع أقل من التكلفة والتأكد من رفض الفاتورة.

## إصلاح مشكلة التاريخ والفلتر اليومي

كانت قاعدة البيانات تحفظ `CURRENT_TIMESTAMP` بتوقيت UTC. لذلك فإن فاتورة أُنشئت الساعة 02:00 فجرًا يوم 25 بتوقيت اليمن كانت تظهر كـ `23:00` يوم 24 عند تفسير الطابع الزمني كتاريخ محلي عادي. تم الحفاظ على القيمة المخزنة دون تغيير، وإضافة `date-utils.js` لتفسير طوابع SQLite كـ UTC وعرضها بتوقيت `Asia/Aden`؛ وبذلك يتحول المثال `2026-08-24 23:00:00` إلى `2026-08-25 02:00`.

كما تم تحديث الفلترة المحلية في الفواتير والطلبات، وإحصاء مبيعات اليوم، ومخطط الطلبات، وعروض المشتريات والمبيعات والموردين وحركات المخزون. وتم توحيد `localISODate` في `filter-utils.js` على توقيت `Asia/Aden`، واستخدام تاريخ العمل المحلي داخل Worker بدل `toISOString()` عند إنشاء القيود وفحص الإغلاق. لا تُعدّل البيانات القديمة؛ بل تُعرض وتُفلتر بالتفسير الزمني الصحيح.

| الحالة | النتيجة الصحيحة بعد الإصلاح |
|---|---|
| إنشاء فاتورة 02:00 يوم 25 | تظهر بتاريخ 25 ووقت 02:00 |
| اختيار فلتر «اليوم» يوم 25 | يشمل الفاتورة ولا يبحث عنها في يوم 24 |
| فاتورة 23:00 يوم 24 UTC | تُعرض محليًا 02:00 يوم 25 |

تم اختبار الحالة الزمنية مباشرة، ونجحت اختبارات الأطقم والمشتريات والفلاتر والفواتير والتقارير وتكامل الموقع، مع بقاء `driver/` دون تعديل.

لم تُنفذ أي كتابة على API الإنتاج أثناء هذه الجولة لعدم توفر جلسة مدير إنتاج موثقة، وبقيت الاختبارات محلية/ثابتة كما هو مطلوب.
